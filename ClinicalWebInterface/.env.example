# Base URL for backend API
# If not set, client will fall back to http://localhost:3000
# Note: API paths are called under /api by the client, e.g., /api/auth/login
REACT_APP_API_BASE_URL=http://localhost:3000
