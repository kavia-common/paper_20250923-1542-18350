# Base URL for backend API. Do not include trailing slash.
# For local dev when backend also on localhost (adjust port as needed):
REACT_APP_API_BASE_URL=http://localhost:3000

# For container networking (e.g., docker-compose/k8s), set to service DNS name:
# REACT_APP_API_BASE_URL=http://BackendServices:3000
